import gensim

default_embedding = ['word2vec', 'FastText']


class NotoriousBig:

    def __int__(self, embedding_switch: 'str', embedding_addr: 'str'):
        self.word_embedding = self.read_embedding(embedding_switch, embedding_addr)

        return

    @staticmethod
    def read_data_tag_label():

        return

    @staticmethod
    def add_new_word(word, model):

        return

    def read_embedding(self, switch, data_addr):
        # if switch in default_embedding:
        #
        # else:
        #     print('such embedding is not available ')

        return